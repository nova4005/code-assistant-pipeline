export PATH="/opt/homebrew/bin:$PATH"

STAGED_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep -E '\.(ts|tsx|js|jsx|mjs|cjs|php|json)$' | grep -v '^test-fixtures/')

if [ -n "$STAGED_FILES" ]; then
  node "$(dirname "$0")/../scripts/pipeline.js" $STAGED_FILES
fi
