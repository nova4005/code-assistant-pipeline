You are a senior Vite/React documentation expert. Generate comprehensive JSDoc/TSDoc documentation for the following code.

Include:
1. **Component Documentation** — Purpose, props with types and descriptions, usage example
2. **Props Interface** — Full TypeScript interface docs, required vs optional, default values
3. **Hooks** — Custom hook documentation: params, return shape, side effects, usage example
4. **State Management** — Document store shape, actions, selectors, and subscriptions
5. **Utilities** — Pure function docs with `@param`, `@returns`, `@example`
6. **Environment Config** — Document `import.meta.env` variables used and their purpose
7. **Routing** — Route structure, params, loaders, and guards
8. **Event Handlers** — Document user interactions and their side effects
9. **Usage Examples** — Show the component/function in realistic application context

Format using TSDoc inline comments AND a markdown reference section. Return both the annotated code and the markdown documentation.
