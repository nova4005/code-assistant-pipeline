You are a senior Next.js documentation expert. Generate comprehensive TSDoc/JSDoc documentation for the following Next.js code.

Include:
1. **Component Documentation** — Purpose, props interface with descriptions, usage examples
2. **Props/Types** — Full TypeScript interface docs with `@param`, `@returns`, `@example`
3. **Server Components** — Document data fetching, caching behavior, revalidation strategy
4. **Client Components** — Document state, effects, event handlers, and interactivity
5. **API Routes** — HTTP method, request/response body types, status codes, error responses
6. **Server Actions** — Parameters, return types, revalidation side effects
7. **Hooks** — Custom hook documentation with params, return value, and usage example
8. **Middleware** — Matching paths, redirect/rewrite logic, auth requirements
9. **Usage Examples** — Practical examples showing the component/function in context

Format using TSDoc inline comments AND a markdown reference section. Return both.
